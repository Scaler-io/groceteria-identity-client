import { CamelCaseSpacerPipe } from './camel-case-spacer.pipe';

describe('CamelCaseSpacerPipe', () => {
  it('create an instance', () => {
    const pipe = new CamelCaseSpacerPipe();
    expect(pipe).toBeTruthy();
  });
});
